# sudoku

## Algorithm:

Implements with C++ (backup/algorithm.cpp).

compile:  
> ```  
> g++ algorithm.cpp -o algorithm -std=c++11 
> ```

run:
> ``` 
> ./algorithm 
> ```

## UI:
Implements with SpriteKit
